name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  release-candidate:
    uses: ./.github/workflows/release-certification.yml
    with:
      module-ref: ${{ github.event.pull_request.head.sha || github.sha }}
